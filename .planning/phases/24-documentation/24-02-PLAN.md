---
phase: 24-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/API.md
autonomous: false

must_haves:
  truths:
    - "User can follow README build instructions on fresh macOS checkout"
    - "User can run README quickstart code and see text rendered"
    - "All features listed in README are implemented and working"
  artifacts:
    - path: "README.md"
      provides: "Verified build and usage instructions"
  key_links:
    - from: "README.md"
      to: "examples/demo.v"
      via: "quickstart code"
      pattern: "v run examples/demo.v"
---

<objective>
Verify README documentation accuracy and fix any discrepancies.

Purpose: Users rely on README for first-time setup; incorrect instructions cause abandonment (DOC-03, DOC-04, DOC-05).
Output: README with verified build instructions, accurate feature list, working code examples.
</objective>

<execution_context>
@/Users/mike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-documentation/24-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build prerequisites and quickstart</name>
  <files>README.md</files>
  <action>
1. **Verify prerequisites (DOC-03):**
   - Check macOS instructions work: `brew install pango freetype pkg-config`
   - Verify the packages are correct (not outdated names)
   - Confirm pkg-config is actually needed

2. **Verify quickstart code (DOC-05):**
   - Extract the quickstart V code block from README
   - Create a temporary file and run with `v -check-syntax`
   - Verify imports match actual module exports
   - Confirm struct fields and function calls are current API

3. **Document any fixes needed:**
   - If quickstart code has outdated API calls, update them
   - If prerequisites are wrong, correct them

Focus on macOS since that's the primary platform (per PROJECT.md).
  </action>
  <verify>
- `v -check-syntax` passes on extracted quickstart code
- Prerequisites section matches actual dependencies
  </verify>
  <done>README build instructions and quickstart code verified working.</done>
</task>

<task type="auto">
  <name>Task 2: Audit feature list accuracy</name>
  <files>README.md</files>
  <action>
Verify each feature claim in README "Features" section (DOC-04):

| Feature Claim | Verification Method |
|---------------|---------------------|
| Complex Layout (Arabic, Hebrew) | Check examples/demo.v has RTL text |
| Advanced Typography (OpenType) | Check typography_demo.v exists |
| Rich Text (span tags) | Check rich_text_demo.v exists |
| High Performance (LRU cache) | Grep for LRU in source |
| Local Fonts (.ttf/.otf) | Check API has font loading |
| Hit Testing | Check layout_query.v has hit test |
| Font Fallback | Check source handles fallback |
| Text Decorations | Check TextConfig has underline/strikethrough |
| High-DPI Support | Check DPI handling in renderer |
| LCD Subpixel AA | Check glyph_atlas.v has subpixel |
| Variable Fonts | Check variable_font_demo.v exists |
| Text Measurement | Check API has text_width/text_height |
| Lists | Check list_demo.v exists |
| Accessibility | Check accessibility module exists |

For any feature NOT implemented, either:
- Remove from README, OR
- Note as "planned" if appropriate (unlikely for audit phase)

Update README if any claims are inaccurate.
  </action>
  <verify>
Each feature claim has corresponding implementation verified.
No false claims remain in README.
  </verify>
  <done>README feature list matches actual implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Verified README build instructions and feature accuracy</what-built>
  <how-to-verify>
1. Review any changes made to README.md
2. Optionally: Run `v run examples/demo.v` to confirm quickstart works
3. Confirm feature list looks accurate
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- README.md passes `v check-md -w README.md`
- Quickstart code compiles: extract to temp file, run `v -check-syntax`
- No feature claims without implementation
</verification>

<success_criteria>
- Build instructions tested and working
- Quickstart code syntax-verified
- All feature claims verified against codebase
- README changes (if any) committed
</success_criteria>

<output>
After completion, create `.planning/phases/24-documentation/24-02-SUMMARY.md`
</output>
